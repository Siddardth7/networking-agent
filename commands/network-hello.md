---
description: Smoke-test command confirming the networking-agent plugin is installed and command routing works.
---

# /network-hello

When this command is invoked, respond with:

> "✅ networking-agent plugin installed. /network-hello command works. Phase 1 smoke test: PASS."

This command will be replaced by the real command stubs in Phase 1 Step 1.2.
