# Trial run — AST SpaceMobile (2026-06-13, v0.2.1)

First fresh-company live run since the v0.2.0 quality work. LinkedIn-only
(email enrichment off; zero Hunter spend). 15 contacts, 30 drafts, 85s.
Artifact: `~/.networking-agent/drafts/ast-spacemobile/2026-06-13-run.md`.

## Scorecard vs the v0.2.0 pass criteria

| Criterion | Target | Result | Verdict |
|---|---|---|---|
| Fabricated metrics (the original P0) | 0 | **0** — no `%`/`+` numeric claims in any draft | PASS |
| Placeholder leaks | 0 | **0** | PASS |
| Held drafts with a populated reason | 100% | **100%** | PASS |
| GENERIC hooks | low | **0/15** | PASS |
| Verbatim-news hooks | 0 | **0** | PASS |
| Hook whitelist | all pass | **15/15** | PASS |
| Opener variety (cap 2/channel) | no over-cap | 15 distinct CONN, 14 distinct POST, **0 over-cap** | PASS |
| Critic hold rate | 20–40% | **7% blocking (2/30); 20% flagged incl. SOFT_FLAG** | BELOW BAND |

Quality codes: 24 OK / 4 SOFT_FLAG / 1 CRITIC_HOLD / 1 HARD_FAIL.

## Why the hold rate is 7%, not the predicted 33%

The 33% prediction came from applying the recalibrated rule to the **old**
2026-06-06 score vectors. This run's drafts are generated by the post-v0.2.0
pipeline — placeholder prevention, single-ask enforcement, intro dedup,
opener variety, and real signal-derived hooks all run **before** the critic.
First-pass quality is genuinely higher, so the critic legitimately holds less.
The gate is not rubber-stamping: it held/flagged exactly the right drafts.

- **CRITIC_HOLD — Ryan McCabe POST** (spec2 one_2 econ2): a real double-ask
  ("are you mostly hands-on... or process challenges?" + "grab 15 minutes").
  Correct hold.
- **HARD_FAIL — Thomas Hobbs CONN**: 201 chars, 1 over the 200 cap. Correct,
  if pedantic — no auto-trim retry exists, so a 1-char overflow is held.
- **4 SOFT_FLAG (all POST)**: multi-ask survived the regen (e.g. Hartley's
  "supplier/process audits vs design reviews?" + "grab 15 minutes"). Surfaced
  to the reviewer, still approvable. Correct.

The clean (OK) connection notes are specific and sendable — e.g. Patrick Blue:
"Saw your expertise in AS9100 and AS9102 requirements — MS AE at UIUC with
composites and structural analysis background. Would value connecting."

## Residual findings (none blocking)

1. **AI tell "exactly the kind of" in 4/30 drafts** — including one OK
   (sendable) draft (Elena W.). The phrase was explicitly named in
   FIX_AGENT_PROMPT for the humanizer but is not in the blocklist. **Clear,
   one-line fix:** add the family to the voice.md / seed blocklist.
2. **Identity-clause boilerplate**: 5/15 connection notes share the
   "MS AE at UIUC, composites and structural analysis background" clause.
   The opener-variety check only normalizes sentence 1, so the repeated
   sentence-2 identity isn't diversified. Arguably fine (identity should be
   consistent), but the notes have a recognizable template.
3. **One thin OK note** — Anthony Barber CONN: "Saw your work in quality
   engineering at AST SpaceMobile" is near-zero personalization beyond the
   title (spec/grou = 2/2). Passed the rule (≤2 weak dims). Not unsafe.
4. **Finder targeting**: one retired contact (Hartley Parent) and one
   hook/person education mismatch (Thomas's "U Michigan-Dearborn grad student"
   signal vs his AST manufacturing role). Minor; Finder-level, not draft-level.

## Verdict

**PASS — clear, large improvement over the 2026-06-06 run; safe to send with
light human review.** Zero fabrication, zero placeholders, real specific hooks,
full opener variety, every hold explained. The 7% hold rate is below the
20–40% heuristic band but for the right reason (better generation), and the
gate catches the genuine multi-ask / length problems. The original safety
goal (nothing bad sends; not everything is held) is met.

## Open Question 2 — resolved

Live hold rate measured at 7% on improved generation; the critic correctly
holds genuine multi-ask drafts and flags borderline ones. The system is no
longer over-holding (was 93%). Remaining tuning is optional, not safety.
