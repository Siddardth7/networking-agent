# Finder trial — AST SpaceMobile (2026-06-28, v0.6.0)

The proving run that closes "develop the Finder like the Drafter" (#10). Live
Finder pipeline (Discover → Enrich → Classify → Hook → Save) on the same company
as the drafter trial (`TRIAL_AST_SPACEMOBILE_2026-06-13.md`), scored on the same
shape of objective, ground-truth-free criteria.

LinkedIn-only (email enrichment off; **zero Hunter/Apollo spend**). Run in an
isolated config dir + DB so the live campaign state in `~/.networking-agent/`
was never touched. Requested 15, **discovered 13** (Apify primary; best-effort
to N — providers exhausted at 13, logged, not silently capped — ROADMAP A1 / #8).
Scorecard generated by `src/eval/finder_scorecard.py`.

## Scorecard vs the parity bar (the drafter trial's hook criteria)

| Criterion | Target | Result | Verdict |
|---|---|---|---|
| Discovery yield | > 0 | **13/15** (best-effort, logged) | PASS |
| GENERIC hooks | 0 | **0/13** | PASS |
| Verbatim-news hooks | 0 | **0/13** | PASS |
| Hook whitelist | all pass | **13/13 (100%)** | PASS |
| Missing LinkedIn | low | **0/13** | PASS |

**Verdict: PASS.** Mirrors the drafter trial's PASS on the same hook bar (0
GENERIC, 0 verbatim-news, full whitelist). The hooks are specific and
signal-derived, not categories — this is the #9 (hook-on-imported / hook-grounding)
work landing on live discovery data:

- "7 years aerospace quality, APQP/PPAP/FMEA/MSA/SPC" (Niall Evans)
- "Managed 600+ suppliers, led RCA/corrective actions using BPSM" (Patrick Blue)
- "20+ years leading NPI in aerospace, medical, automotive" (Miguel Valencia)
- "Fastening Systems engineer with aerospace structural fasteners qualification"
  (Alba Regojo Catalan)

## Classify spread (reported, not gated)

- Persona: **PEER_ENGINEER 7, SENIOR_MANAGER 6** — a clean two-way split with no
  RECRUITER/ALUMNI false positives on an all-engineer set.
- Focus: **MANUFACTURING 9, MATERIALS 2, PEER 2** — correct for an SQE-heavy
  query (the `finder_role_keywords` lead with quality/supplier/manufacturing).

Spread is observational only. Classify *accuracy* is owned by the labeled-set
scorecard (#4, `CLASSIFY_SCORECARD_2026-06-26.md`); this trial confirms the
distribution is sane and the two non-engineer personas didn't misfire.

## Why this is parity, not just "a PASS"

The drafter trial proved the **draft** quality bar (no fabrication, no
placeholders, real hooks, every hold explained). This trial proves the **Finder
feeds that bar**: every contact arrives with a whitelist-passing, signal-derived
hook and a sane persona/focus, so the drafter starts from real anchors rather
than GENERIC. The two halves now meet the same objective standard on live data.

## Residual findings (none blocking)

1. **Hook mid-word truncation** — Sainath Auty's hook ends "…large assembly
   stru" (cut at the 80-char `hook_signal` ceiling in `_classify_contact`). It
   still passes the whitelist and reads as a real signal, but a word-boundary
   trim would be cleaner. *One-line fix candidate:* trim `hook_signal` at the
   last space ≤ 80, mirroring `_trim_to_char_limit` in the drafter. → small
   follow-up.
2. **Two Tier-3 fallbacks** — vamshi joriga and John Ramsey got the title-bucket
   hook "your manufacturing and quality background" (their snippets had no
   extractable specific signal). Correct, safe behavior (better than GENERIC),
   but lower specificity than the other 11. Inherent to sparse profiles.
3. **`title` captures the full LinkedIn headline** — e.g. Ernest P.'s title is a
   7-clause tagline, Octavina's includes a quote. This is the Apify/Serper data
   shape, not a Finder bug; the classifier and hook still handled them. Flag for
   the importer/finder ingest audit (#24) if a title-vs-headline split is wanted.
4. **Targeting precision** — 3 of 13 (Octavina — Li-battery researcher; Miguel —
   medical/automotive NPI; Kiryl — EMEA QA) read as keyword matches whose
   *current AST employment* isn't confirmed in the snippet, vs the 7 with an
   explicit "…at AST SpaceMobile" headline. Same class as the drafter trial's
   residual #4 (one retired contact). Finder-level precision, not a hook defect;
   the human selection gate is the intended filter. Candidate for the
   referral-likelihood ranking work (#11).

## Verdict

**PASS — the Finder is at parity with the Drafter's quality bar.** On live AST
SpaceMobile data: full discovery yield within provider limits (best-effort,
logged), 100% whitelist-passing hooks with zero GENERIC and zero verbatim-news,
specific signal-derived anchors, and a sane persona/focus spread with no
misfires. Residuals are precision/polish (hook trim, targeting confidence),
not safety — and route to existing issues (#24, #11). The "develop the Finder
like the Drafter" goal (#10) is met.

## Reproduce

```
# isolated run — leaves ~/.networking-agent/ untouched, zero email spend
python -c "from src.eval.finder_scorecard import run_finder_trial; \
print(run_finder_trial('ast-spacemobile', limit=15).render_markdown())"
```
